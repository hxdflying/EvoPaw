aiohttp>=3.9
pydantic>=2.0
PyYAML>=6.0
lark-oapi>=1.0.0
croniter>=2.0.0
requests
Pillow
prometheus_client
asyncpg>=0.29.0
pgvector>=0.2.0

# Claude Agent SDK (替代 CrewAI)
claude-agent-sdk>=0.1.0

# OpenAI 兼容客户端 (context_mgmt 摘要压缩 + indexer 向量化，调用通义 DashScope)
openai>=1.0.0

# pgvector 写入 (indexer + search_memory 脚本)
psycopg2-binary>=2.9.0

# 搜索工具 (替代百度千帆)
tavily-python>=0.3.0

# 论文检索
arxiv>=2.1.0
PyPDF2>=3.0.0

# 开发与测试依赖
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
aiohttp-test-utils>=0.0.1
